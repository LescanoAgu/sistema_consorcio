rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Permite acceso a la carpeta "consorcios" solo a usuarios autenticados
    match /consorcios/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}